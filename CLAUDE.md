# CLAUDE.md — ASSAY working context

ASSAY is a docs-first demonstrator: JIPOE-derived knowledge, typed and banded, exploited by planning machinery honestly. Read `README.md` for the map; read `docs/assay-vignette.md` → `docs/assay-walkthrough.md` before touching anything.

## Where next steps live — check before starting work

1. **GitHub issues on this repo are the live queue.** Every planned next step, with its trigger condition ("after PR #N merges", "after Stage-1 exit"), is an open issue. List open issues before picking up work; close issues as part of finishing, not after.
2. **Plan-level intent** lives in the canonical docs and is authoritative over any issue: `docs/assay-delivery-plan.md` (slices, dependencies, waves), `docs/assay-build-plan.md` (stage exits, demo moments), `docs/assay-concept.md` §6 (register candidates awaiting ratification), `docs/assay-findings.md` §4 (ledger open items).
3. **Session-bound watchers do not survive sessions.** PR subscriptions and scheduled check-ins belong to the session that created them; if you are a new session picking up mid-stream, re-check open PR state yourself.

## Non-negotiables (from the register — do not re-derive these)

- **Register-first (ASSAY-DEC-2):** decisions originate in `docs/assay-register.md`, nowhere else. If your work implies a new decision, record a *candidate* in `docs/assay-concept.md` §6 and flag it — never assert it in a peer document.
- **Research-first (ASSAY-DEC-11):** a stage's implementation does not start until its research note exists in `docs/research/`. No exceptions; a missing note is the gate, not a formality.
- **Banded honesty (constitution II, seam G2):** no scalar from an assessed source appears unbanded anywhere — responses, stored objects, surfaces, logs. Code review rejects violations.
- **Identifiers are frozen** (`K*`, `C*`, `R*`, `FE-*`, `P*`, `O-*` — vignette §8/§9). The wireframes and gallery must render vignette identifiers only; drift is a defect.
- **Process from Stage 1 onward:** build slices run through spec-kit (`/speckit.specify` → plan → tasks per SPEC-##, seeded from the build plan's stage exits) — decided 2026-07-12. Stage 0 predates this and is not backfilled.
- **Batch propagation:** a change to one canonical doc sweeps its peers (including `assay-ui-wireframes.html` and this file's "current phase" line) in the same change.

## Commands

```
npm install          # once
npm run gen          # LinkML schema → src/generated/types.ts (never edit generated files)
npm run typecheck    # tsc --noEmit
npm test             # vitest — includes fixture validation and coverage-row protection
npm run gallery      # regenerates docs/assets/gallery/index.html from fixtures
npm run bench        # canonicalise+hash at dense-world scale (research note 00 §3)
```

The oracle cases (vignette §9, `O-1…O-4`) are hand-computed and must never be regenerated from implementation output.

## Current phase (update this line when it changes)

Stage 0 complete; PR #8 merged to `main`. Register batch 3 ratified (ASSAY-DEC-22…27, closing concept §6.6–6.11) — walkthrough and findings ledger now canonical, G6 a standing invariant. Register batch 4 ratified (ASSAY-DEC-28…34, closing concept §6.12–6.18) — sparse channels, SPA shell, glow, in-browser pipeline, blog cadence, home-page currency, flow-view behaviours all settled; constitution 1.2.0; delivery plan three-category taxonomy. Stage 1 built (issue #10): research note `01-knowledge.md` (DEC-16 floor); SPEC-05 implemented (`src/{seam,deltas,encoding,lint,knowledge}.ts` + `components/{refusalBanner,s1Table}.ts`); all four Stage-1 exits demonstrated on Meridian. Stage 2 built: research note `docs/research/02-compile.md` (DEC-11 gate — decides the **sparse channel representation** from the Stage-0 dense-world numbers + ATP 2-01.3 MCOO doctrine, retiring dense `Channel.cells`); SPEC-06 specified via spec-kit (`specs/006-compile/`) and **implemented** (`src/compile.ts` + `components/channelTrace.ts`; `fixtures/vignette-config.json`; sparse-channel schema regen; 107 tests, typecheck clean; compile demo moment renders in the gallery). All three Stage-2 exits demonstrated on Meridian (K12 contested ⇒ compile refuses `contested_knowledge`, resolve⇒recompile; byte-identical stamp; every channel traces back to named knowledge via `compiled_into` + `source`). The sparse-channel schema change is ratified as ASSAY-DEC-29 (batch 4). Stage 3 scorer built: research note `docs/research/03-score-plan.md` (DEC-11 gate — **decides the four-stop verdict mapping as signed margin bands, signs-only**: robust iff m_lo>0, marginal iff m_lo=0, tight iff m_lo<0≤m_hi, violated iff m_hi<0 — the unique O-3-satisfying rule with no interior cut; also settles `(cell,time)` route evaluation over a time-expanded graph, closing note 02 §3's forward flag, and reserves ε-dominance for SPEC-08 plan comparison); SPEC-07 specified via spec-kit (`specs/007-score/`) and **implemented** (`src/{interval,materialise,metrics,score}.ts` + `components/s2Matrix.ts`; `fixtures/plans.json` canned handful; no schema change — verdict/score/plan shapes already exist; 139 tests, typecheck clean; the honest-matrix demo moment renders in the gallery). The correctness leg holds on Meridian: oracle O-1 (`[9,13]`), O-2 (robust, margin `[15,19]`), O-3 (four-stop sweep changes only at band edges 9/13) reproduced exactly, and O-4 propagation-honesty (G6) under property-based testing (`fast-check`). SPEC-07 asserts no new register decision (the mapping is decided in the research note under the latitude vignette §9/O-3 delegated to Stage-3). Stage 3 handful built: SPEC-08 specified via spec-kit (`specs/008-handful/`) and **implemented** (`src/{dominance,generate,handful}.ts` + `components/handfulStrip.ts`; new seam types; no schema change) over the same `03-score-plan.md` note (§5 — the DEC-11 gate, deciding **banded ε-non-domination** and Meridian's **four generation axes**). `/plan/handful` = generate (seeded fan-out over approach × suppression × causeway × extraction) → score (reuses the SPEC-07 scorer) → organise (ε-non-domination on the conservative interval order, ε defaults to 0, diversity cap by axis Hamming — never a scalar, DEC-19). Both Stage-3 handful exits demonstrated on Meridian: `/plan/handful` returns **4 genuinely distinct plans** for the base world (each pair mutually non-dominated; the survivors split cleanly along the two trade axes — approach trades C1↔C2, suppression trades C3↔C4), and **same stamp + seed ⇒ identical handful** (G1); 163 tests, typecheck clean; the honest-matrix demo moment now renders the *generated* handful plus a derived `distinct_because` strip (organised, not authored). SPEC-08 asserts no new register decision (the concept §6.2 handful-axes open question is closed by the research note §5.2 under Stage-3 latitude). Stage 4 built (thesis B live): research note `docs/research/04-relaxation.md` (DEC-11 gate — **decides the formalism as preemptive lexicographic priority over the `must/should/prefer` tiers with NO weights**: weighted CSP / MAX-SAT / Archimedean goal programming rejected on honesty grounds by DEC-19, lexicographic goal programming chosen; returns the **inclusion-minimal correction-set frontier** not one optimum, ranked least-worst-first with the must-sacrifice kept-but-last; `sacrificed` = exactly the `violated` commitments; a **content-neutral, stated tie-break** for same-tier sacrifices because encoding a civil-harm-vs-force-protection ranking is the value judgement DEC-19 forbids; command-language narratives per MDMP/JP 5-0; the note also corrects course honestly — the SPEC-08 BASE generator routes clear of the banded regions and cannot enter the mined water, so SPEC-09 authors an R3m-responsive candidate set while the **sacrifices stay computed** by the reused scorer); SPEC-09 specified via spec-kit (`specs/009-relaxation/`) and **implemented** (`src/{tiers,relaxCandidates,relax}.ts` + `components/s3Cards.ts`; new seam types; no schema change — `RelaxationReport`/`RelaxationCandidate`/`Plan` already exist; 187 tests, typecheck clean; the "least-worst, never silence" demo moment renders in the gallery). The Stage-4 exit holds on Meridian: `/relax` over R3m returns **three inclusion-minimal least-worst candidates sacrificing {C4,C5}, {C3,C5}, {C2,C5}** (sets corrected by SPEC-20 — see below) (each `sacrificed` non-empty, computed by the scorer — re-scoring each plan violates exactly its `sacrificed` set; the `{C2,C4,C5}`/`{C3,C4,C5}` supersets are inclusion-dropped), ranked least-worst-first (the C2 `must`-sacrifice last but present), the same-tier C3/C4 tie stated in `tie_break`, every card in command language and traced back via `cited_in`/`sacrificed_in` (G3), never empty and never a silent drop (G4). SPEC-09 asserts no new register decision (the formalism is decided *by* DEC-19; the report shape *by* knowledge model §5/§8 and seam §7/§G; latitude is Stage-4-delegated under vignette §6/§9). Front-end lane opened: **SPEC-16 (interactive surfaces) built** over research note `docs/research/05-surfaces.md` (DEC-11 gate) and concept §6 candidates 16–18 (ratified as ASSAY-DEC-32/33/34 in batch 4 — tabs/SPA, live in-browser pipeline + editable surfaces via the shell/pure-component split, glow = G6 made visible). The published app is now a live SPA running the *real* pipeline in-browser (esbuild bundle `npm run build:app` → `docs/assets/app/`): four role tabs (J-2/planner/commander/observer) over one client store (`src/app/{state,shell,glow,bootstrap}.ts`), full editability gated by the existing firewall/lint/contested refusals (dishonest edits refuse, G2/G5), a value-keyed, row/cell-level cross-tab + 10s glow (each pure component stamps `data-glow-id`/`data-glow-sig`; a unit glows only when its displayed value moves — resolving K12 glows just the K12a row, not K1–K9; no under/over-report, G6), and a one-hop "informs/influenced-by" trace menu over a per-`edge_type` orientation map (`src/traceView.ts`); the eight `src/components/*` stayed pure (SPEC-14 extractability intact) and gained a per-component legend (`src/components/legends.ts`, the "data pill key"). Verified in Chromium end-to-end (K12 resolve → planner/commander glow → matrix recompute → trace menu) and by Node suites (`traceView`/`glow`/`app-bootstrap`; 202 tests, typecheck clean). Shipped with its comms §6 blog article (`docs/blog/`) embedding the live demonstrator, and the SPEC-16 spec-kit set (`specs/016-surfaces/`). No schema change. Communications scaffold landed alongside (PR #22): `docs/status.yml` is the single source of truth (§5), read and honesty-gated by `scripts/build-site.ts`; the blog + standalone-embed pipeline (`docs/blog/`, `scripts/build-embeds.ts`, `npm run embeds`) ships with its first pattern-setter article (the band pill, a live embed of the shipped component), now joined by the SPEC-16 "the seam goes live" article embedding the live demonstrator; `docs/blog/backlog.md` tracks the articles owed. Comms plan v0.5 records the Jekyll→static-assembler build-path divergence (§8), ratified as ASSAY-DEC-30 (batch 4, blog cadence is register-blessed authoring, not a spec-kit feature). **System-flow infographic built (SPEC-14 flow-view sub-slice):** research note `docs/research/07-flow-view.md` (DEC-11 gate — decides the three-zoom Shneiderman structure, the fan-out-vs-recompute distinction, and that auto-recompute is honest for an *explainer* because the protected property is attribution, not manual triggering); specified via spec-kit (`specs/014-flow-view/`) and **implemented** (`src/flow.ts` + `src/flowPage.ts` + `scripts/build-flow.ts`, `npm run flow`; reuses the shipped `bandPill`/`provenanceChip`/`refusalBanner`/`channelTrace`/`s2Matrix`/`s3Cards`/`handfulStrip`; typecheck clean). One self-contained, offline-clean embed (`docs/assets/flow/index.html`) in two homes — the in-app S4 "systems-map" mode and a public Pages explainer (`site/flow.html`), wired into `scripts/build-site.ts` and linked from an `assay-home.html` card. It drives the **real in-browser seam** (compile/score/handful/relax/knowledge/deltas/trace) over the frozen Meridian tableau, pre-rendering every reachable tour beat + bounded sandbox state at build time (the blessed band-pill embed pattern — no runtime crypto, no bundler, zero network), with L0/L1/L2 zoom and tour/sandbox modes. Buildable-now scope (Stages 0–4) is computed; the two unbuilt computations — staleness fan-out (thesis F, Stage 6) and selection (`/select`, later) — are **scripted from the walkthrough and visibly labelled "scripted — not yet computed"**, never faked (DEC-4). Auto-recompute is attribution-visible (stamp flip + delta row + gate pulse), never silent; the truly-silent "you-are-the-optimiser" toggle stays deferred for an SME. Acceptance intent AS-1…AS-11 (spec §9) are real tests (`tests/flow.test.ts`). The four behavioural candidates (concept §6.15) are **ratified as ASSAY-DEC-31** (batch 4; behaviours a/b/c ratified, d deferred for SME reaction). Home-page-currency (concept §6.12) assessed: the view demonstrates theses A/B end-to-end interactively but moves no thesis from *planned* to *demonstrated* (A/B already `explored`; C/D/E/F stay gated/scripted), so `status.yml` thesis states are unchanged — an `updates` entry records the landing. **Stage 5 built (thesis C live):** research note `docs/research/06-robustness.md` (DEC-11 gate — **decides the minimax (worst-case) robustness posture**: scenario-weighted rejected by DEC-15/19 scenario_weight firewall; minimax regret rejected because it launders absolute failure into relative shortfall; the worst-case verdict is a real verdict on a real scenario, not a weighted blend); SPEC-10 specified via spec-kit (`specs/010-robustness/`) and **implemented** (`src/robustness.ts` + `components/scenarioStrip.ts` + legends entry; no schema change — `CommitmentVerdict.scenario` and `CompiledWorld.scenario` already support multi-scenario labelling; 233 tests, typecheck clean; the "don't plan on most-likely" demo moment renders in the gallery). The Stage-5 exit holds on Meridian: `RobustnessService` scores each plan against each scenario world via the existing `ScoreService.score()` (DEC-10, scorer-in-a-loop — no new engine), assembles the plan × commitment × scenario verdict tensor, and computes worst-case (minimax) per plan×commitment. The **thesis-C exhibit** is reproducible from fixtures: P1 (strait-early) C1/C2 verdicts are `robust` under BASE but `violated` under R2 (Strait Denial mines the strait); P2 (sweep-first) holds C4 `robust` across all scenarios where P1 does not — the plans trade differently, and the commander sees the trade at a glance in the scenario strip. The scenario strip renders collapse markers (▼) on degraded cells and a comparability warning banner when `stamps_compatible` is `false`. Wired into the live app (`src/app/state.ts`) and the gallery (`scripts/build-gallery.ts`). SPEC-10 asserts no new register decision (the minimax posture is decided by the research note under DEC-15/19 latitude). Stage 6 built (theses D, E, F live): research note `docs/research/08-analysis.md` (DEC-11 gate — **decides tornado perturbation for sensitivity** (not Monte Carlo/Sobol — DEC-15 forbids distributional assumptions), **COA-pair band separation for discrimination** (not Shannon entropy/VOI — scenario-weight firewall), and **transitive trace walk for staleness** (not recomputation — constitution)); SPEC-11/12/13 specified via spec-kit (`specs/011-sensitivity/`, `specs/012-discrimination/`, `specs/013-staleness/`) and **implemented** (`src/{sensitivity,discrimination,staleness}.ts`; new seam types; no schema change — `expected_answers`, `collection`, `single_source` already exist on KnowledgeObject; 267 tests, typecheck clean). The three Stage-6 exits hold on Meridian: **K8 tops the sensitivity ranking** with `single_source: true` (its band-edge perturbation changes C4 verdicts via the battery fire-control threshold — thesis E); **K11 ranks above K13 on discrimination** despite higher cost (R1/R2 expected-answer bands disjoint vs K13's overlapping — thesis D); **K5 staleness walk flags exactly the storm-channel-dependent verdicts** (P1-C2, P2-C1, P2-C2) and nothing else (thesis F). SPEC-11/12/13 assert no new register decision (sensitivity uses the existing `knowledge_overrides` perturbation hook; discrimination uses the existing `expected_answers` miniature event matrix DEC-18; staleness uses the existing `TraceStore.walk` with the `traceView.ts` orientation map). Batch propagation swept: `docs/status.yml` and `docs/assay-home.html` updated to show Stage 5 and 6 done, theses C/D/E/F explored; the spine diagram's "Analyse" box green; the stage tracker all checkmarks through Stage 6. Three new rendering components (`src/components/{sensitivityTable,discriminationTable,stalenessFlags}.ts`) wired into the gallery (`scripts/build-gallery.ts`) and the live app (`src/app/state.ts`). **Stage 7 built (narratives & wall-projection):** research note `docs/research/09-narratives.md` (DEC-11 gate — **decides the five narrative scripts as DSM walk-throughs with explicit scripting principles**: never narrate a midpoint, never narrate a weighted sum, use the verdict as a category, state the tie-break, quote the doctrine; also decides **wall-projection mode as CSS-only** `data-projection="wall"` toggle — large type ≥24px, forced dark, auto-follow, reduced chrome; also audits the band pill against the uncertainty-communication literature — deterministic construal, anchoring, false precision, endpoint labelling, scale overload all resisted by construction); SPEC-17 specified (`specs/017-narratives/`) and **implemented** (`src/narratives.ts` + `src/app/shell.ts` modified; narrative runner with selector, beat navigation, presenter notes panel, doctrinal quotes, centrepiece markers, and scripting-principles reminder; wall-projection CSS toggle; 327 tests, typecheck clean). The Stage-7 exit holds: each narrative runs as a scripted ≤10-minute demo from a cold start, offline, assembling the per-stage demo moments banked at Stages 1–6 (DEC-23). **SPEC-18 (cross-cutting research spike, issue #42) shipped:** the "From Q&A to COA" blog article (`docs/blog/posts/2026-07-14-qa-to-coa.html`) + a three-panel live embed (`scripts/build-embeds-qa.ts` → `docs/blog/embeds/qa-to-coa/`, wired into `npm run embeds`) tracing K3/K6/K7 through the real compile+score pipeline as the three-pattern taxonomy (band-propagating K6→C4 with a live G6 slider; geometric-only K3→C3 the honest gap; route-dependent miss K7→C4). No `src/` change; every value asserted by `tests/k3-trace.test.ts`. **SPEC-19 built (spatial & temporal COA visualisation, issue-less depth slice, review-first):** research note `docs/research/10-spatial-temporal.md` (DEC-11 gate — **decides the banded-surface rule**: an assessed channel renders as a two-stop split fill + width-keyed hatching with exact `[lo,hi]` on hover, never a single stop (G2; distribution-assuming encodings — HOPs, quantile dotplots, gradient fades — rejected because a Band has no interior, DEC-15); **and the temporal rule**: validity/task windows as discrete extents, lapse marked never carried, stated peaks rendered as quoted attributed annotations never curves (thesis F); surveys 5 perspectives and recommends map + timeline composed, with the **exposure profile** staircase as connective tissue); implemented as pure seams (`src/mapProject.ts` + `components/{coaMap,coaTimeline}.ts` — projections of fixtures, SPEC-14 extractability held) + the interaction layer (`src/app/{coaViz,coaVizShell,coaVizBootstrap}.ts`, `npm run coa-viz` → self-contained `docs/assets/coa-viz/index.html`, linked from a home card + `site/coa-viz.html`). The mockup runs the **real in-browser pipeline**: scrub = pure selection (no recompute, no glow); dragging a waypoint authors a new `Plan` version (DEC-20/21) and re-scores through the real `ScoreService` — C4 flips tight→robust when ANVIL leaves `fac_waters`, glow value-keyed (G6), byte-identical to a from-scratch run (G1); dishonest inputs (inverted band, waiver-stripped K8) refuse in place (G2/G5). The SPEC-18 honest gap is now geometry: the `[12,36]` band-hour riser in `fac_waters` (K6→C4), the untouched `air_defence` box (K7 route-miss), no profile at all for C3 (K3 unread). 379 tests, typecheck clean; blog article `2026-07-15-where-the-plan-meets-the-water.html` embeds the live mockup. **Review-first honoured, then closed: the SME checkpoint endorsed the UI model (2026-07-15, findings ledger ASSAY-FIND-7), so register batch 6 ratified ASSAY-DEC-36 (closing concept §6.21(a)–(d)) and T15 is built — the surface is promoted to a fifth shared "Spatial · COA" tab in the live SPA over the same single store (`src/app/state.ts` coa panels + `setStep`/`moveWaypoint`/`shiftWindow`; `src/app/shell.ts` tab + spatial clock + drag wiring + glow-silent scrub; `tests/app-coa-tab.test.ts`): the map refuses with the compile while K12 is contested (G5) and appears, glowing, on resolve; a drag re-scores the canned P1/P2 through the same pipeline; DEC-32's four role tabs stand (the spatial tab is a cross-role surface, not a role) and DEC-31(d) binds; 383 tests, typecheck clean.** No schema change. **Register batch 5 ratified (issue #41, ASSAY-DEC-35):** the civil-harm-quantification candidate SPEC-18 surfaced (concept §6.20) is decided — **civil harm stays a geometric red line, never a quantified metric**. C3 (`civil_harm_exposure`, kind `fires`) counts whether FE-FALCON fires into the populated `port_district`; it does **not** read the `civil_density` channel and the proposed `weighted_civil_harm` metric is declined. Whether firing into 55,000 is "worse" than 5,000 is a commander/SME value judgement DEC-19 forbids the system from encoding; the honest gap SPEC-18 documented (K3's band compiled but not read — removing K3 leaves every C3 verdict identical, `tests/k3-trace.test.ts` 5–5b) is affirmed as a design feature. No schema/`src/`/constitution change (the decision declines a metric and affirms Principle II / DEC-19, projecting no new rule). Batch propagation swept: register batch 5, concept §6.20 struck through, findings ledger gains ASSAY-FIND-6 (DEC-19 held under a live challenge), SPEC-18 §5 Q1 resolved, `status.yml` updates feed. **SPEC-20 built (compile-overlay precedence, fix-class — review 2026-07-14 §3.6/A5, slice S-B):** research note `02-compile.md` **§6 amendment** (DEC-11 gate — decides **layered precedence: excursion beats base**, window first, within-layer tie order unchanged; the layer is *derived* — `override.source === world.scenario` — no schema change; audits the frozen tableau: the only visible flip is the causeway case); specified via spec-kit (`specs/020-compile-precedence/`) and **implemented** (`src/materialise.ts` layered `channelAt` + `src/engine.ts` `ENGINE_VERSION = '0.2.0'`; robustness lineage check now includes the engine). The one computed-world/narrative contradiction is gone: R3/R3m causeway reads return the demolition state, C5 scores `violated`, the Stage-4 sacrifice sets are {C4,C5}/{C3,C5}/{C2,C5} (minimality/ranking/tie-break unchanged; candidate narratives state the causeway's fate in command language), BASE and O-1…O-4 byte-stable, G6 property holds; batch propagation swept (vignette §6/§7, walkthrough beat 4, build-plan exit note, flow spec + wireframes, gallery/flow captions, `status.yml`) and all published artifacts regenerated at engine 0.2.0. Register candidate **concept §6.22** (excursion-beats-base precedence) awaits the next batch. **SPEC-21 built (JIPOE anchoring, review §4.1/A7 · addendum slice S-A):** research note `01-knowledge.md` **amended** (DEC-11 gate — fixes the `JipoeStep` vocabulary as JP 2-01.3's four steps verbatim, the per-object assignments for all seventeen Meridian K objects restating the §3 audit one object at a time and resolving K10 → step 3, the singular *originating*-step rule with usage left to the trace graph, and the `missing_jipoe_step` lint posture: warning-level like the width lint, recalibrated after Checkpoint 1, but `observed` **not** exempt — origin applies to facts too); specified via spec-kit (`specs/021-jipoe-anchoring/` spec/plan/tasks) and **implemented**: schema gains the `JipoeStep` enum + optional `jipoe_step` slot on `KnowledgeObject` (knowledge-model §4/§11 + `schema/*.yaml` in lockstep, `npm run gen` regen — **register candidate concept §6.23, flagged not asserted**, awaiting the next batch); fixtures annotated and **pinned by an oracle-style assignment table** (`tests/fixtures.test.ts` — drift fails); `src/lint.ts` `jipoeStepLint` wired into create/supersede with warnings on the write response **and the published delta** (`Delta.warnings?`, seam contract v0.5 §3/§10); the provenance chip renders the step **in words** wherever provenance renders (`provenanceChip(prov, jipoe_step)` swept through `s1Table`/`channelTrace`/`flow.ts`/gallery/QA-embeds; S1 glow sig carries the step, G6) with a `jipoe` legends entry; 395 tests, typecheck clean; no banded/scalar semantics change, no oracle/verdict/coverage-row change. Its **DEC-30 companions shipped alongside** (comms category, review A1/A2/A6): the SME-facing doctrine page (`docs/assay-doctrine.html` → site `doctrine.html` + Home card — the §4.1 crosswalk with its step column now machine-audited, and the §4.4 five-entry divergence register); the citation-hardening pass over notes 02/03/05/08 (Cooke & Halsey/Orda & Rom; Dubois & Prade/Ben-Haim/Fishburn; Bainomugisha et al./Hermans et al.; Fishburn/Troffaes) with the two date corrections (Savage 1951, note 06; Lipkus & Hollands 1999, note 09); and the README status fix (A6 — the public face no longer claims implementation has not started). **SPEC-22 built (attention ordering, review §3.5/M11 · addendum slice S-C):** research note `docs/research/11-attention.md` (DEC-11 gate — **decides the interval order as the one honest ordering for likelihood bands** (`lo(a) > hi(b)` ⇒ strictly above; overlap incl. nesting ⇒ honestly incomparable, level; every scalar sort key rejected by DEC-15/19; stochastic orders rejected — a Band has no interior; Hurwicz rejected — a smuggled α), the longest-chain attention-layer stratification, the queue tie-break (**exact** band-equality trigger, conservative pair lift over the symmetric difference, stated in the rendering, never across overlap, DEC-18 never overridden, composed in the queue assembly around an untouched `DiscriminationService`), and the contested/missing-weight postures (unranked, never uniform-defaulted; a disputed weight orders nothing)); specified via spec-kit (`specs/022-attention-ordering/`) and **implemented** (`src/attention.ts` — delegates comparison to `dominance.ts`, no new machinery; `components/scenarioStrip.ts` attention block above an untouched verdict grid — K14a–c on a shared 0–100 % track, provenance welded, "orders attention — never compiles" label; `components/discriminationTable.ts` stated tie-break line; legends `attention`/`weight_tiebreak`; `src/app/state.ts` seeds K14a–c — never passed to compile — and wires both surfaces; gallery wired). Both Stage exits hold on Meridian: **R1 (45–70 %) strictly above the level, visibly overlapping {R2 (20–40 %), R3 (10–25 %)}**, and **"attention only" machine-checked** — a K14 band edit changes zero stamps/verdicts/memberships while the attention block moves; import isolation (attention/strip ↮ compile/score/dominance/relax) is a source-level test; 438 tests, typecheck clean. Register candidate **concept §6.24** (the two-behaviour sharpening — the complete set; any third use is a new candidate) flagged, not asserted, awaiting the next batch. No schema change; no oracle, verdict, or coverage-row change. **SPEC-23 built (collection discrimination v2 — sharper where-to-look, review §3.4/B7 · addendum slice S-E):** research note `08-analysis.md` **§7 amendment** (DEC-11 gate — decides the **operative-pair derivation**: a scenario pair is operative iff some plan×commitment verdict diverges across it in the SPEC-10 tensor, computed never curated, restricted to the requested COA vocabulary, comparability guard before derivation, K14 firewalled out; the **three-way separation classification** disjoint/partial/nested with the endpoint boundaries stated — touching ⇒ partial mirroring O-3's edge discipline, endpoint-sharing containment ⇒ nested, and "cannot discriminate" precisely glossed as the inner COA having no exclusive region; and **ExpectedAnswer provenance** — the full `Provenance` shape, optional slot, warning-level `missing_expected_answer_provenance` lint, the event matrix was the only major assessed content without a chip); specified via spec-kit (`specs/023-discrimination-v2/`) and **implemented** (schema + regen; `fixtures/knowledge.json` K11/K13 rows provenanced — assessed·moderate/assessed·low, J-2 red cell; `src/discrimination.ts` `deriveOperativePairs`/`classifySeparation` + operative-led sort with nested pairs excluded from could-discriminate emphasis; `src/seam.ts` `OperativePairs`/`SeparationClass`/mode/statement + `DiscriminationRequest.tensor?`; `src/lint.ts` + `src/knowledge.ts` wiring; `components/discriminationTable.ts` v2 — mode line with witnesses, operative column, classification words+marks, expected-band block with provenance chips; legends `separation_class`/`operative_pair`; `src/app/state.ts` passes the live robustness tensor; gallery reordered so the tensor conditions the published ranking). Both spec exits hold: **K11 > K13 reproduces on the operative ranking over the REAL frozen tensor** ({R1,R2} derives operative from P1's C1/C2 flips, witnesses rendered) and the **pinned divergence case** (strong-inert-pair vs moderate-operative-pair) shows v1 and v2 orderings differing exactly as specified; v1 numeric separations byte-identical (asserted); degenerate states (no tensor, incomparable stamps, one live scenario, no divergence, all-operative) render honest statements; 439 tests, typecheck clean; seam contract v0.6. Register candidates **concept §6.25/§6.26** flagged, not asserted, awaiting the next batch (DEC-2). **SPEC-24 built (the Decision Support Matrix surface — decisions in time, review §4.2/M1/B1 · addendum slice S-D, the keystone):** research note `docs/research/12-decision-support.md` (DEC-11 gate — decides the **two-class DP predicate** (scenario-divergent over the adversary vocabulary, per-row operative pairs, K14 never enters / margin-class `tight`·`marginal` under the selected world; uniform robust AND uniform violated are non-DPs — the latter is relax's business), the **commit-step rule per metric kind** (first route leg whose metric-channel read differs across the live COA worlds, at exactly the points `metrics.ts` reads; horizon step worded *world-decided* for `state` metrics; stated absence for margin-class-only rows — never an invented step), **LTIOV = commit_step − lead (lead 0, stated) on the scenario clock** (DEC-17) with the three-state answerable-in-time predicate (in time with slack a step count / the honest red **"cannot answer in time" with its arithmetic, never dropped** / unstated never assumed answerable), discriminator reuse (SPEC-12/23 `classifySeparation`, nested excluded, the named intelligence-gap sentence), and **world-level tripwire scope** (consumed knowledge lapsing before the commit step — the trace graph's honest granularity, worded as such)); specified via spec-kit (`specs/024-decision-support-matrix/`) and **implemented** as a thin orchestration (`src/decisionSupport.ts` — RobustnessService tensor + discrimination classification + `channelAt` + validity windows, no new engine, DEC-10; new seam movement types; **no schema change**) + the pure surface (`src/components/dsmTable.ts` + `legends.ts` entries) wired as an S2-adjacent **commander panel "decisions in time"** (`src/app/state.ts` — refuses with the compile G5, re-derives + value-keyed glow G6, the absent `/select` selection stated) and a gallery moment. The pinned Meridian exhibit holds (`tests/decisionSupport.test.ts`, the note-§7 table as oracle): **P2 sweep-first holds the port decision (C1) open to step 10 — DET KINGFISHER's step-8 report arrives in time with 2 steps of slack — where P1 strait-early commits at step 4, a day before recon can report** (the DSM shows, computed, why sweep-first buys decision space); P2-C2 renders the honest red (8 > 2); C5 is world-decided at the horizon with the **K9 lapse (36 < 56) tripwire**; every row `cited_in`-edged to its evidence (G3), byte-identical re-runs (G1), no urgency scalar anywhere (DEC-19), **no tasking affordance** (the KINGFISHER/C6 discipline, FR-007). 493 tests, typecheck clean. Register candidates **concept §6.27/§6.28** (the DP derivation rule; the surface + seam addition) flagged, not asserted, awaiting the next batch (DEC-2). **Focused transitive dependency-graph view built (issue #24, the review's C4 / S-G's full-screen big brother):** research note `docs/research/13-dependency-graph.md` (DEC-11 gate — decides the **transitive walk under the `EDGE_ORIENTATION` map** as a composition of the one-hop `traceView.neighbours` reading applied BFS-depth-by-depth, not a new engine (DEC-5/10); **symmetric `contests` edges render on both flanks, once each, cycle-guarded**; the **layered node-link "river" + details-on-demand sidebar** idiom (degenerate Sugiyama layout at bounded Meridian scale, Shneiderman/Munzner/PROV inherited from note 07); the **truncation-honesty rule** — the G4 no-silent-drop discipline lifted to a view: breadth cap "+ K more" **and** depth cap "⋯ deeper chains continue beyond depth N", never silent; and **placement as a full-screen overlay off the SPEC-16 trace menu, not a sixth tab** — DEC-32's four role tabs and DEC-36's single shared-surface tab stand, Observer sub-mode the stated fallback). Implemented (`src/depGraph.ts` `buildDepGraph`/`nodeDetail` + `components/{depGraphRiver,depGraphSidebar}.ts` + shell overlay wiring reachable from every component, `tests/depGraph.test.ts`; the design mock `docs/assets/dep-graph-mocks/index.html`). Read-only projection of the existing trace graph — no schema change, no oracle/verdict/coverage-row change, moves no thesis (`status.yml` unchanged); 495 tests, typecheck clean, app bundle rebuilt. **Process note:** the slice was prototyped mock-first and *built ahead of its research note* — an inversion of DEC-11's research-first order, now closed by note 13, which decides the design on its merits and **audits the build** (§7), correcting the one gap it found (the depth cap was silently truncating — now rendered honestly). Register candidate **concept §6.29** (the dependency-graph surface + its transitive-traversal reading) flagged, not asserted, awaiting the next batch (DEC-2); the DEC-11 inversion is recorded as a findings-ledger process observation. **SPEC-25 built (verdict legibility & the argument surface, review §3.1/§3.3 · addendum slice S-F):** research note `docs/research/14-legibility.md` (DEC-11 gate for **US2 only**) decides the **consequence-preview contract** — a preview is the **real pipeline run over a byte-faithful shadow of the store** (`AppState.fork` clones store/trace/deltas — cheap because the store is immutable, SPEC-01 — and wires fresh services over the clone), applied through the same service methods and the same firewall as a commit, **never an estimate** (DEC-10): the **previewed diff equals the post-commit glow set element-for-element** (G1/DEC-34, `src/preview.ts` `previewAct`→`changedGlowUnits` over the value-keyed sig maps), the shadow **persists nothing, emits no delta, mutates no stamp**, and a **dishonest armed act previews its refusal** (G2/G5); it is client-side orchestration of existing calls — **no seam change** (a `POST /preview` contract flagged as a *further* candidate). The other four moves re-render banked output: **US1** the **public verdict legend** (`src/components/verdictLegend.ts` — the four-stop mapping laid open CRT-style, illustrated by the **frozen oracle O-3 sweep** `[9,13]`×`at_most T` over 8→14 → `[violated,tight×4,marginal,robust]`, the marginal-is-measure-zero footnote, **oracle-derived not live**, a mapping never a score DEC-19; keyed `verdict_legend` in `legends.ts` via `verdictLegendFor`, reachable from every verdict-bearing panel); **US3** **relaxation cards v2** (`s3Cards.ts` gains a derived **"puts at risk"** line — verdict deltas vs the stated incumbent **P1-under-R3m**, degradations short of violation, computed by the reused scorer never authored; `sacrificed` **still exactly the violated set** SPEC-09 unchanged; numbered, individually trace-linked reasons; on Meridian the R3m candidates are **Pareto-clean** so the residue is **honestly empty**, stated-absence never fabricated); **US4** the **challenge affordance** (`src/components/challengePanel.ts` + `AppState.challenge` — the SPEC-11 sensitivity ranking **scoped to a verdict**, single-source flags co-shown, deep-linked to the S1 row, the honest "no single band-edge movement flips this" insensitive message; **no new compute**, same sensitivity stamp; under BASE the honest C4 flipper is **K6** (the documented band-propagation, SPEC-18), K8 genuinely insensitive there — surfacing it would be false); **US5** **per-role action menus** (`src/roleMenus.ts` `ROLE_VERBS` source-of-truth + `src/components/roleMenu.ts` — each role's legal write verbs marked **live / runs-on-recompute / not-yet-built** never a dead button DEC-4, **observer read-only**, menus reorganise never restrict DEC-33; the Spatial tab a cross-role *surface* not a role, DEC-32 stands). The pure verdict mapping was extracted to `src/verdictMap.ts` (re-exported by `score.ts`) so the legend reads the same rule the scorer does; store/trace/delta `clone()` enable the fork. Wired into the live SPA (`state.ts` legend on verdict panels + at-risk cards + `challenge`; `shell.ts` arm/commit/cancel preview toggle + ghost, challenge-on-verdict-cell, per-tab role menus) and a gallery moment. 523 tests, typecheck clean; no schema/oracle/verdict/coverage-row change, no new scalar (DEC-19). Register candidates **concept §6.30/§6.31** (the consequence-preview interaction class; per-role action menus as a standing shell element) flagged, not asserted, awaiting the next batch (DEC-2). **SPEC-26 research gate authored (issue-less depth slice, review slice S-G):** research note `docs/research/15-replay.md` (DEC-11 gate — decides the **state-at-seq reconstruction rule** as the event-sourcing fold `state(n) = fold(deltas[1..n])`, implemented as the immutable store (DEC-21) filtered by a derived, non-authoritative `created_seq` index — a filter, never a rollback, byte-equal by G1; **replayed refusals re-derive** from `state(n)` rather than being stored/elided; the **narrative-runner unification** onto `(seq, tab, note)` scrub-path waypoints, retiring the bespoke beat mechanism while preserving the SPEC-17 presenter contract; and the **recursive-trace tooltip depth cap fixed at 3** — note 13's transitive walk depth-capped, agreeing with `TraceStore.walk`, dead ends at every depth (G3), an honest counted remainder handing off to the trace drawer / issue #24; framed against AAR practice and event-sourcing honesty, decision-time replay distinguished from and composed with SPEC-19's world-time scrub). **Register batch 7 ratified (ASSAY-DEC-37…39, closing concept §6.32–6.34):** the decision-history replay surface (DEC-37), the recursive-trace tooltip affordance (DEC-38), and narratives-as-scrub-paths (DEC-39) — all read-only projections over existing machinery (SPEC-05 delta log, SPEC-16 glow/trace menu, DEC-21 store), no schema change, no seam change, no new engine (DEC-10); SPEC-26 implementation is now unblocked. **Register batch 8 ratified (ASSAY-DEC-40…47, closing concept §6.22–6.29):** the review-series slices built-then-ratified in one session — compile-overlay precedence (DEC-40, `ENGINE_VERSION` 0.2.0), `jipoe_step` (DEC-41, schema, touches DEC-6), attention ordering's two-behaviour closed set (DEC-42), `ExpectedAnswer.provenance` (DEC-43, schema, touches DEC-9/DEC-18), operative-pair conditioning (DEC-44), the DP derivation rule (DEC-45), the DSM surface + `POST /analyse/decision-support` (DEC-46), and the transitive dependency-graph surface (DEC-47); two carry schema changes already regenerated in their builds, the rest read-only projections or materialisation-semantics; the §6.29 mock-first DEC-11 inversion recorded as a findings-ledger process observation with DEC-11's research-first order standing for future slices. The concept §6 candidate queue is now empty but for the horizon item §6.19 (forward derivation, issue #43). **Next: SPEC-26 plan/tasks/build; issue #24 close-out; the SME-checkpoint (#11) and REMIT-handoff (#12) queue items; horizon forward-derivation (#43).**
